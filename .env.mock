# Mock API environment — copy to .env.local to run without a real backend
#
#   cp .env.mock .env.local && pnpm dev
# OR pass inline:
#   NEXT_PUBLIC_MOCK_API=true pnpm dev

NEXT_PUBLIC_MOCK_API=true
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/qqq/v1
